#include "bits.h"

#include <stdio.h>

int main(){
    int a = INT_MAX;
    int b = INT_MIN;
    unsigned int c = UINT_MAX;
    printf("Il valore int di a è: %d\n", a);
    printf("Il valore int di b è: %d\n", b);
    printf("Il valore int di c è: %u\n", c);

    return 0;
}